node_modules/
dist/
.astro/
.env
.env.*
!.env.example

# Local override for projects.config (real handles, not committed)
projects.config.local.ts

# Private docs (deployment configs with real values, etc.) — never committed
docs/private/

# Local-only Claude Code context (personal/sensitive notes)
CLAUDE.local.md

.DS_Store
*.log
